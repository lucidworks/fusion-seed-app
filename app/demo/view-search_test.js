'use strict';

describe('fusionSeed.viewTwitterSearch module', function() {

  beforeEach(module('fusionSeed.viewTwitterSearch'));

  describe('view-search controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewSearchCtrl = $controller('ViewTwitterSearchCtrl');
      expect(viewSearchCtrl).toBeDefined();
    }));

  });
});